# This file is autogenerated by maturin v1.9.6
# To update, run
#
#    maturin generate-ci github --pytest
#
name: CI

on:
  push:
    branches:
      - main
      - master
    tags:
      - '*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        run: |
          set -e
          sudo apt-get update
          sudo apt-get install -y python3-dev
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.91.0
          components: clippy,rustfmt
      - name: cargo fmt
        run: cargo fmt --check
      - name: cargo clippy
        run: cargo clippy --all-targets --all-features
  linux:
    needs: [checks]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - { runner: ubuntu-22.04, target: x86_64, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t" }
          - { runner: ubuntu-22.04, target: x86, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t" }
          - { runner: ubuntu-22.04, target: aarch64, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t", no_default_features: true }
          - { runner: ubuntu-22.04, target: armv7, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t", no_default_features: true }
          - { runner: ubuntu-22.04, target: s390x, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t", no_default_features: true }
          - { runner: ubuntu-22.04, target: ppc64le, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t", no_default_features: true }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --interpreter ${{ matrix.platform.interpreter }} ${{ matrix.platform.no_default_features && '--no-default-features' || '' }}
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: auto
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-${{ matrix.platform.target }}
          path: dist
      - name: pytest
        if: ${{ startsWith(matrix.platform.target, 'x86_64') }}
        shell: bash
        run: |
          set -e
          curl -LsSf https://astral.sh/uv/install.sh | sh
          export PATH="$HOME/.local/bin:$PATH"
          source $HOME/.cargo/env
          for pyver in ${{ matrix.platform.interpreter }}; do
            echo "::group::Python $pyver"
            uv venv ".venv-$pyver" --python "$pyver"
            source ".venv-$pyver/bin/activate"
            uv pip install xmltodict-rs --no-index --find-links dist --force-reinstall
            uv pip install --group test
            pytest
            deactivate
            echo "::endgroup::"
          done
  musllinux:
    needs: [checks]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - { runner: ubuntu-22.04, target: x86_64, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t" }
          - { runner: ubuntu-22.04, target: x86, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t" }
          - { runner: ubuntu-22.04, target: aarch64, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t" }
          - { runner: ubuntu-22.04, target: armv7, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t" }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --interpreter ${{ matrix.platform.interpreter }} --no-default-features
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: musllinux_1_2
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-musllinux-${{ matrix.platform.target }}
          path: dist
      - name: pytest
        if: ${{ startsWith(matrix.platform.target, 'x86_64') }}
        uses: addnab/docker-run-action@v3
        with:
          image: alpine:latest
          options: -v ${{ github.workspace }}:/io -w /io
          run: |
            set -e
            wget -qO- https://astral.sh/uv/install.sh | sh
            export PATH="$HOME/.local/bin:$PATH"
            uv venv
            source .venv/bin/activate
            uv pip install xmltodict-rs --no-index --find-links dist --force-reinstall
            uv pip install --group test
            pytest
      - name: pytest
        if: ${{ !startsWith(matrix.platform.target, 'x86') }}
        uses: uraimo/run-on-arch-action@v2
        with:
          arch: ${{ matrix.platform.target }}
          distro: alpine_latest
          githubToken: ${{ github.token }}
          install: |
            apk add python3
            wget -qO- https://astral.sh/uv/install.sh | sh
          run: |
            set -e
            export PATH="$HOME/.local/bin:$PATH"
            uv venv
            source .venv/bin/activate
            uv pip install xmltodict-rs --find-links dist --force-reinstall
            uv pip install --group test
            pytest
  windows:
    needs: [checks]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - { runner: windows-latest, target: x64, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t" }
          - { runner: windows-latest, target: x86, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t" }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
          architecture: ${{ matrix.platform.target }}
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --interpreter ${{ matrix.platform.interpreter }}
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-windows-${{ matrix.platform.target }}
          path: dist
      - name: pytest
        if: ${{ matrix.platform.target == 'x64' }}
        shell: bash
        run: |
          set -e
          curl -LsSf https://astral.sh/uv/install.sh | sh
          export PATH="$HOME/.local/bin:$PATH"
          for pyver in ${{ matrix.platform.interpreter }}; do
            echo "::group::Python $pyver"
            uv venv ".venv-$pyver" --python "$pyver"
            source ".venv-$pyver/Scripts/activate"
            uv pip install xmltodict-rs --no-index --find-links dist --force-reinstall
            uv pip install --group test
            pytest
            deactivate
            echo "::endgroup::"
          done
  macos:
    needs: [checks]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - { runner: macos-14, target: aarch64, rust_target: aarch64-apple-darwin, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t" }
          - { runner: macos-15-intel, target: x86_64, rust_target: x86_64-apple-darwin, interpreter: "3.10 3.11 3.12 3.13 3.13t 3.14 3.14t" }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.91.0
          targets: ${{ matrix.platform.rust_target }}
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --interpreter ${{ matrix.platform.interpreter }}
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-${{ matrix.platform.target }}
          path: dist
      - name: pytest
        shell: bash
        run: |
          set -e
          curl -LsSf https://astral.sh/uv/install.sh | sh
          export PATH="$HOME/.local/bin:$PATH"
          source $HOME/.cargo/env
          for pyver in ${{ matrix.platform.interpreter }}; do
            echo "::group::Python $pyver"
            uv venv ".venv-$pyver" --python "$pyver"
            source ".venv-$pyver/bin/activate"
            uv pip install xmltodict-rs --no-index --find-links dist --force-reinstall
            uv pip install --group test
            pytest
            deactivate
            echo "::endgroup::"
          done
  sdist:
    needs: [checks]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: wheels-sdist
          path: dist

  release:
    name: Release
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch' }}
    needs: [linux, musllinux, windows, macos, sdist]
    permissions:
      # Use to sign the release artifacts
      id-token: write
      # Used to upload release artifacts
      contents: write
      # Used to generate artifact attestation
      attestations: write
    steps:
      - uses: actions/download-artifact@v4
      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: 'wheels-*/*'
      - name: Publish to PyPI
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        uses: PyO3/maturin-action@v1
        env:
          MATURIN_PYPI_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        with:
          command: upload
          args: --non-interactive --skip-existing wheels-*/*
